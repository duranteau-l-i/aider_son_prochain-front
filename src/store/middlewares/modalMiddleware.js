// import { MODAL, modalShow } from 'store/reducers/modal';

// const modalMiddleware = store => next => action => {
//   switch (action.type) {
//     case MODAL:
//       store.dispatch(modalShow(false, false));
//       break;
//     default:
//       next(action);
//   }
// };

// export default modalMiddleware;
